services:
  api:
    build: .
    env_file: .env
    ports:
      - "8150:8000"
    volumes:
      - ./data:/app/data

  ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
    environment:
      MEDCODER_API_URL: http://api:8000
    ports:
      - "8651:8501"
    depends_on:
      - api
